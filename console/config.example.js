// Copier en `config.js` et renseigner avec les valeurs du projet Firebase
// (Paramètres du projet → Vos applications → Web).
//
// Ces valeurs ne sont pas secrètes : elles identifient le projet et sont
// embarquées dans n'importe quelle application web Firebase. Ce qui protège la
// console, c'est le claim `agent` exigé côté serveur, pas la discrétion de ce
// fichier.
export const firebaseConfig = {
  apiKey: "…",
  authDomain: "…",
  projectId: "…",
  appId: "…",
};

export const REGION = "europe-west1";
