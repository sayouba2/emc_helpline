{
  "name": "emc-helpline-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for EMC Helpline — see docs/backend-plan.md",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test",
    "serve": "npm run build && firebase emulators:start --only functions",
    "grant-agent": "tsx scripts/grant-agent.ts"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "tsx": "^4.19.0"
  }
}
